claude --permission-mode acceptEdits "@PRD.md @progress.txt \\
1. Read the PRD and progress file. \\
2. Find the next incomplete task and implement it. \\
3. Commit your changes. \\
4. Update progress.txt with what you did. \\
ONLY DO ONE TASK AT A TIME."

=== PROGRESO DEL PROYECTO ===

✅ PASO 1: Setup inicial del proyecto (COMPLETADO)
   - Creado package.json con scripts dev, build, start
   - Creado tsconfig.json con configuración ES2022 y ESNext
   - Creada estructura de directorios: src/models, src/repositories, src/controllers, src/routes
   - Instaladas dependencias: express, @types/express, typescript, tsx
   - Creado .gitignore para node_modules y dist
   - Commit: ed41a86

✅ PASO 2: Crear modelo y repositorio (COMPLETADO)
   - Creada interfaz Pokemon en src/models/pokemon.model.ts con campos: id, name, type, level
   - Creado repositorio en src/repositories/pokemon.repository.ts con:
     * Array en memoria con 3 pokemones iniciales (Bulbasaur, Charmander, Squirtle)
     * Variable nextId para auto-incremento
     * Métodos: getAll(), getById(), create(), update(), deleteById()
   - TypeScript compila sin errores

✅ PASO 3: Crear controladores y rutas (COMPLETADO)
   - Creado pokemon.controller.ts con funciones controladoras:
     * listPokemons - retorna todos los pokemones
     * getPokemon - retorna pokemon por ID o 404
     * createPokemon - crea nuevo pokemon con validación, retorna 201
     * updatePokemon - actualiza pokemon existente o retorna 404
     * deletePokemon - elimina pokemon o retorna 404
   - Validación básica de campos y tipos en cada controlador
   - Manejo de errores con try-catch y responses apropiados
   - Creado pokemon.routes.ts con definición de rutas:
     * GET / → listPokemons
     * GET /:id → getPokemon
     * POST / → createPokemon
     * PUT /:id → updatePokemon
     * DELETE /:id → deletePokemon
   - TypeScript compila sin errores

⏳ PASO 4: Crear servidor Express (SIGUIENTE)

⏳ PASO 5: Prueba manual de endpoints (PENDIENTE)